<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chit Chat</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body>
    <% if (locals.users) { %>
        <div class="min-h-screen gap-5 bg-gray-800   flex flex-wrap items-center  justify-center  ">
            <% users.forEach(user=> { %>
                <div
                    class="container lg:w-2/6 xl:w-2/7 sm:w-full md:w-2/3    bg-white  shadow-lg    transform   duration-200 easy-in-out">
                    <div class="flex justify-center px-5  -mt-12">
                        <img class="h-32 w-32 bg-white p-2 rounded-full   " src=<%=user.profile.profileImg%>
                        alt="Profile Image" />

                    </div>
                    <div class="flex flex-col items-center ">
                        <div class="text-center px-14">
                            <h2 class="text-gray-800 text-3xl font-bold">
                                <%=user.name.firstName%>
                                    <%=user.name.lastName%>
                            </h2>
                            <p class="text-gray-400 mt-2 hover:text-blue-500">
                                <%=user.email%>
                            </p>
                            <p class="mt-2 text-gray-500 text-sm">
                                <%=user.profile.status%>
                            </p>
                        </div>
                        <form action="/chat/profile/<%=user._id%>" method="post">
                            <button class="w-max bg-black rounded-3xl text-white px-4 py-2 my-5">
                                Add Contact
                            </button>
                        </form>
                    </div>
                </div>
                <% }) %>
        </div>
        <% } %>
</body>

</html>